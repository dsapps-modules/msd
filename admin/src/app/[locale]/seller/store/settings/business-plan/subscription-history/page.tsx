import CustomLayout from "@/components/layout/CustomLayout";
import Store from "@/components/screen/seller-section/store";

const SubscriptionHistoryRoot = () => {
  return (
    <CustomLayout>
      <Store />
    </CustomLayout>
  );
};

export default SubscriptionHistoryRoot;