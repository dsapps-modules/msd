import CustomLayout from "@/components/layout/CustomLayout";
import Notices from "@/components/screen/seller-section/store/settings/notices";

const NoticesRoot = () => {
  return (
    <CustomLayout>
      <Notices />
    </CustomLayout>
  );
};

export default NoticesRoot;