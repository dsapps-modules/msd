import CustomLayout from "@/components/layout/CustomLayout";
import ChatList from "@/components/screen/seller-section/store/chat/list";

const ChatListRoot = () => {
  return (
    <CustomLayout>
      <ChatList />
    </CustomLayout>
  );
};

export default ChatListRoot;