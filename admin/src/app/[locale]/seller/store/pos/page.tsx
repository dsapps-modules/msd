import CustomLayout from "@/components/layout/CustomLayout";
import POS from "@/components/screen/seller-section/store/pos";

const POSRoot = () => {
  return (
    <CustomLayout>
      <POS />
    </CustomLayout>
  );
};

export default POSRoot;