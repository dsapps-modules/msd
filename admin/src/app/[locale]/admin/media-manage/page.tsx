import CustomLayout from "@/components/layout/CustomLayout";
import MediaManage from "@/components/screen/admin-section/MediaManage";

const MediaManageRoot = () => {
  return (
    <CustomLayout>
      <MediaManage />
    </CustomLayout>
  );
};

export default MediaManageRoot;
