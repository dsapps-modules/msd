import CustomLayout from "@/components/layout/CustomLayout";
import POSSettings from "@/components/screen/admin-section/pos/settings";

const POSSettingsRoot = () => {
  return (
    <CustomLayout>
      <POSSettings />
    </CustomLayout>
  );
};

export default POSSettingsRoot;