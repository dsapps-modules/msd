import CustomLayout from "@/components/layout/CustomLayout";
import POS from "@/components/screen/admin-section/pos";

const POSRoot = () => {
  return (
    <CustomLayout>
      <POS />
    </CustomLayout>
  );
};

export default POSRoot;