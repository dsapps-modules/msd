import CustomLayout from "@/components/layout/CustomLayout";
import Pages from "@/components/screen/admin-section/pages";

const PagesRoot = () => {
  return (
    <CustomLayout>
      <Pages />
    </CustomLayout>
  );
};

export default PagesRoot;
