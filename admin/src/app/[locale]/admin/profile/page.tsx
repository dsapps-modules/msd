import CustomLayout from "@/components/layout/CustomLayout";
import ProfileSettings from "@/components/screen/user-panel/profile-settings";

const ProfileRoot = () => {
  return (
    <CustomLayout>
      <ProfileSettings />
    </CustomLayout>
  );
};

export default ProfileRoot;