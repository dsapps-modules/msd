import CustomLayout from "@/components/layout/CustomLayout";
import Store from "@/components/screen/admin-section/store";

const StoreRoot = () => {
  return (
    <CustomLayout>
      <Store />
    </CustomLayout>
  );
};

export default StoreRoot;
