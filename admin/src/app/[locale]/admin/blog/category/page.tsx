import CustomLayout from "@/components/layout/CustomLayout";
import BlogCategory from "@/components/screen/admin-section/blog/category";

const BlogCategoryRoot = () => {
  return (
    <CustomLayout>
      <BlogCategory />
    </CustomLayout>
  );
};

export default BlogCategoryRoot;
