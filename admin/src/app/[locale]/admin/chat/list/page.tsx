import CustomLayout from "@/components/layout/CustomLayout";
import ChatList from "@/components/screen/admin-section/chat/list";

const ChatListRoot = () => {
  return (
    <CustomLayout>
      <ChatList />
    </CustomLayout>
  );
};

export default ChatListRoot;