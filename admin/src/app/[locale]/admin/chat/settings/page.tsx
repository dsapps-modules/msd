import CustomLayout from "@/components/layout/CustomLayout";
import ChatSettings from "@/components/screen/admin-section/chat/settings";

const ChatSettingsRoot = () => {
  return (
    <CustomLayout>
      <ChatSettings />
    </CustomLayout>
  );
};

export default ChatSettingsRoot;