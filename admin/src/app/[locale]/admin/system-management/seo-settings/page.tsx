import CustomLayout from "@/components/layout/CustomLayout";
import SEOSettings from "@/components/screen/admin-section/seo-settings";

const SEOSettingsRoot = () => {
  return (
    <CustomLayout>
      <SEOSettings />
    </CustomLayout>
  );
};

export default SEOSettingsRoot;
