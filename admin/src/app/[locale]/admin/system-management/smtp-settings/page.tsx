import CustomLayout from "@/components/layout/CustomLayout";
import SMTPSettings from "@/components/screen/admin-section/smtp-settings";

const SettingsRoot = () => {
  return (
    <CustomLayout>
      <SMTPSettings />
    </CustomLayout>
  );
};

export default SettingsRoot;