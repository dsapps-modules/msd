import CustomLayout from "@/components/layout/CustomLayout";
import Slider from "@/components/screen/admin-section/slider";

const SliderListRoot = () => {
  return (
    <CustomLayout>
      <Slider />
    </CustomLayout>
  );
};

export default SliderListRoot;