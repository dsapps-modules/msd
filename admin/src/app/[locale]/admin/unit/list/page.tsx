import CustomLayout from "@/components/layout/CustomLayout";
import Unit from "@/components/screen/admin-section/unit";

const UnitRoot = () => {
  return (
    <CustomLayout>
      <Unit />
    </CustomLayout>
  );
};

export default UnitRoot;