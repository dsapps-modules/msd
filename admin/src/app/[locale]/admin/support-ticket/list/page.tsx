import CustomLayout from "@/components/layout/CustomLayout";
import SupportTicket from "@/components/screen/admin-section/support-ticket";

const SupportTicketRoot = () => {
  return (
    <CustomLayout>
      <SupportTicket />
    </CustomLayout>
  );
};

export default SupportTicketRoot;