import CustomLayout from "@/components/layout/CustomLayout";
import Notifications from "@/components/screen/admin-section/notifications";

const NotificationsRoot = () => {
  return (
    <CustomLayout>
      <Notifications />
    </CustomLayout>
  );
};

export default NotificationsRoot;
