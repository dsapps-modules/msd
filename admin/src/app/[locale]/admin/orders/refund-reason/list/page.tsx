import CustomLayout from "@/components/layout/CustomLayout";
import RefundReason from "@/components/screen/admin-section/orders/refund-reason";

const RefundReasonRoot = () => {
  return (
    <CustomLayout>
      <RefundReason />
    </CustomLayout>
  );
};

export default RefundReasonRoot;