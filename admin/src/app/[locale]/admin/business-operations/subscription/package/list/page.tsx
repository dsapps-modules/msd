import CustomLayout from "@/components/layout/CustomLayout";
import Package from "@/components/screen/admin-section/business-operations/subscription/package";

const PackageRoot = () => {
  return (
    <CustomLayout>
      <Package />
    </CustomLayout>
  );
};

export default PackageRoot;