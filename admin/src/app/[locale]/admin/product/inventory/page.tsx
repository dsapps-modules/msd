import CustomLayout from "@/components/layout/CustomLayout";
import Inventory from "@/components/screen/admin-section/products/inventory";

const InventoryRoot = () => {
  return (
    <CustomLayout>
      <Inventory />
    </CustomLayout>
  );
};

export default InventoryRoot;