import CustomLayout from "@/components/layout/CustomLayout";
import Request from "@/components/screen/admin-section/products/request";

const RequestRoot = () => {
  return (
    <CustomLayout>
      <Request />
    </CustomLayout>
  );
};

export default RequestRoot;