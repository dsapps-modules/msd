import CustomLayout from "@/components/layout/CustomLayout";
import Export from "@/components/screen/admin-section/products/export";

const ExportRoot = () => {
  return (
    <CustomLayout>
      <Export />
    </CustomLayout>
  );
};

export default ExportRoot;