import CustomLayout from "@/components/layout/CustomLayout";
import Attribute from "@/components/screen/admin-section/attribute";

const AttributeRoot = () => {
  return (
    <CustomLayout>
      <Attribute />
    </CustomLayout>
  );
};

export default AttributeRoot;